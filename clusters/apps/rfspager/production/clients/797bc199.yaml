---
apiVersion: v1
kind: Service
metadata:
  name: internal-797bc199
  namespace: production
spec:
  type: ExternalName
  externalName: 797bc199.${SECRET_CLUSTER_DOMAIN}

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rfspager-797bc199
  namespace: production
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: &domain rfspager-797bc199.${SECRET_CLUSTER_DOMAIN}
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: internal-797bc199
                port:
                  number: 3000
  tls:
    - hosts:
        - *domain
      secretName: *domain
