---
apiVersion: v1
kind: Service
metadata:
  name: internal-797bc199
spec:
  type: ExternalName
  externalName: 797bc199.${SECRET_CLUSTER_DOMAIN}

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rfspager-797bc199
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  rules:
    - host: rfspager-797bc199.${SECRET_CLUSTER_DOMAIN}
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: internal-797bc199
                port:
                  number: 3000
